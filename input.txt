let v = [1, 2, 3, 4, 5];

fn find(arr, target) {
    for x in arr {
        if (x > target) {
            return Some(x);
        }
    }
    return None;
}

let res = find(v, 3);
let val = match res {
    Some(v) => v,
    None => 0
};
print(val);