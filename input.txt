fn find(arr, target) {
    for x in arr {
        if x > target {
            return Some(x);
        }
    }
    return None;
}

let double_num = fn(x) { return x * 2; };
let result = double_num(2);

let g = [1, 3, 5, 2, 4];
let result1 = find(g, result);