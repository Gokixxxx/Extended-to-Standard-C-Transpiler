======================================================================
测试 #45: print match 表达式结果
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
let opt = Some(42);
        let val = match opt { Some(v) => v, None => 0 };
        print(val);
        let r6 = 0;

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

int main() {
    Option_i32 opt = Some_i32(42);
    int val;
    if (opt.is_some) {
        val = opt.value;
    } else {
        val = 0;
    }
    printf("%d\n", val);
    int r6 = 0;
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['42', '0']
实际输出: ['42', '0']

======================================================================