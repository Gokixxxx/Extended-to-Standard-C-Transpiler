======================================================================
测试 #28: 闭包作为参数传递（自动包装）
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
fn apply_twice(f, x) {
            return f(f(x));
        }
        let add3 = fn(x) => x + 3;
        let r6 = apply_twice(add3, 5);

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

static int __fn_1(int x) {
    return (x + 3);
}
static int __fn_1_closure(void *__env, int x) {
    (void)__env;
    return __fn_1(x);
}

int apply_twice(Closure_i32_i32 f, int x) {
    return f.fn(f.env, f.fn(f.env, x));
}

int main() {
    int (*add3)(int) = __fn_1;
    Closure_i32_i32 __t1 = {NULL, __fn_1_closure};
    int r6 = apply_twice(__t1, 5);
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['11']
实际输出: ['11']

======================================================================