======================================================================
测试 #31: 方法带额外参数
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
struct Rectangle {
            width: i32,
            height: i32
        }

        impl Rectangle {
            fn scale(&self, factor) {
                return self.width * self.height * factor;
            }
        }

        let rect = new Rectangle { width: 3, height: 4 };
        let r3 = rect.scale(5);

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

typedef struct {
    int width;
    int height;
} Rectangle;

int Rectangle_scale(Rectangle *self, int factor) {
    return (((self)->width * (self)->height) * factor);
}

int main() {
    
    Rectangle rect = (Rectangle){3, 4};
    int r3 = Rectangle_scale(&(rect), 5);
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['60']
实际输出: ['60']

======================================================================