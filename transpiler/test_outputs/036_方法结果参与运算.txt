======================================================================
测试 #36: 方法结果参与运算
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
struct Num {
            val: i32
        }

        impl Num {
            fn double(&self) {
                return self.val * 2;
            }

            fn triple(&self) {
                return self.val * 3;
            }
        }

        let n = new Num { val: 5 };
        let r10 = n.double() + n.triple();

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

typedef struct {
    int val;
} Num;

int Num_double(Num *self) {
    return ((self)->val * 2);
}

int Num_triple(Num *self) {
    return ((self)->val * 3);
}

int main() {
    
    Num n = (Num){5};
    int r10 = (Num_double(&(n)) + Num_triple(&(n)));
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['25']
实际输出: ['25']

======================================================================