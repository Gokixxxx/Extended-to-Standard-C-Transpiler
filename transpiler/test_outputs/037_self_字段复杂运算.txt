======================================================================
测试 #37: self 字段复杂运算
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
struct Math {
            a: i32,
            b: i32
        }

        impl Math {
            fn compute(&self) {
                return self.a * self.a + self.b * self.b;
            }
        }

        let m = new Math { a: 3, b: 4 };
        let r11 = m.compute();

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

typedef struct {
    int a;
    int b;
} Math;

int Math_compute(Math *self) {
    return (((self)->a * (self)->a) + ((self)->b * (self)->b));
}

int main() {
    
    Math m = (Math){3, 4};
    int r11 = Math_compute(&(m));
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['25']
实际输出: ['25']

======================================================================