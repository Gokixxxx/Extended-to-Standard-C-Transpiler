======================================================================
测试 #14: 闭包作为参数 apply_twice(doubled, 3)
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
fn apply_twice(f, x) {
            return f(f(x));
        }
        let doubled = fn(n) => n * 2;
        let r5 = apply_twice(doubled, 3);

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

static int __fn_1(int n) {
    return (n * 2);
}
static int __fn_1_closure(void *__env, int n) {
    (void)__env;
    return __fn_1(n);
}

int apply_twice(Closure_i32_i32 f, int x) {
    return f.fn(f.env, f.fn(f.env, x));
}

int main() {
    int (*doubled)(int) = __fn_1;
    Closure_i32_i32 __t1 = {NULL, __fn_1_closure};
    int r5 = apply_twice(__t1, 3);
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['12']
实际输出: ['12']

======================================================================