======================================================================
测试 #18: for 循环内闭包
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
let arr1 = [1, 2, 3];
        let sum1 = 0;
        for j in arr1 {
            let f2 = fn() => j;
            let r10 = f2();
            sum1 = sum1 + r10;
        }
        let r11 = sum1;

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

struct __env_1 {
    int j;
};

static int __fn_1(void *__env) {
    struct __env_1 *env = (struct __env_1 *)__env;
    int j = env->j;
    return j;
}

int main() {
    Vec_i32 arr1 = vec_new_i32();
    vec_push_i32(&arr1, 1);
    vec_push_i32(&arr1, 2);
    vec_push_i32(&arr1, 3);
    int sum1 = 0;
    {
        int __t1 = 0;
        for (; __t1 < vec_len_i32(arr1); __t1++) {
            int j = vec_get_i32(arr1, __t1);
            struct __env_1 *f2_env = malloc(sizeof(struct __env_1));
            f2_env->j = j;
            Closure_i32 f2 = {f2_env, __fn_1};
            int r10 = f2.fn(f2.env);
            sum1 = (sum1 + r10);;
            free(f2.env);
        }
    }
    int r11 = sum1;
    free(arr1.data);
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['6']
实际输出: ['6']

======================================================================