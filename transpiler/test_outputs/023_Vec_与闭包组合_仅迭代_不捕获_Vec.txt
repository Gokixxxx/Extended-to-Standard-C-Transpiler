======================================================================
测试 #23: Vec 与闭包组合（仅迭代，不捕获 Vec）
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
let arr2 = [1, 2, 3];
        let sum2 = 0;
        for j in arr2 {
            let f7 = fn() => j;
            sum2 = sum2 + f7();
        }
        let r18 = sum2;

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

struct __env_1 {
    int j;
};

static int __fn_1(void *__env) {
    struct __env_1 *env = (struct __env_1 *)__env;
    int j = env->j;
    return j;
}

int main() {
    Vec_i32 arr2 = vec_new_i32();
    vec_push_i32(&arr2, 1);
    vec_push_i32(&arr2, 2);
    vec_push_i32(&arr2, 3);
    int sum2 = 0;
    {
        int __t1 = 0;
        for (; __t1 < vec_len_i32(arr2); __t1++) {
            int j = vec_get_i32(arr2, __t1);
            struct __env_1 *f7_env = malloc(sizeof(struct __env_1));
            f7_env->j = j;
            Closure_i32 f7 = {f7_env, __fn_1};
            sum2 = (sum2 + f7.fn(f7.env));;
            free(f7.env);
        }
    }
    int r18 = sum2;
    free(arr2.data);
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['6']
实际输出: ['6']

======================================================================