======================================================================
测试 #19: while 循环内闭包
======================================================================

----------------------------------------------------------------------
【输入源码】
----------------------------------------------------------------------
let i1 = 0;
        let wsum = 0;
        while (i1 < 3) {
            let f3 = fn() => i1;
            let r12 = f3();
            wsum = wsum + r12;
            i1 = i1 + 1;
        }
        let r13 = wsum;

----------------------------------------------------------------------
【生成的 C 代码】
----------------------------------------------------------------------
#include "option.h"
#include "vec.h"
#include "closure.h"

struct __env_1 {
    int i1;
};

static int __fn_1(void *__env) {
    struct __env_1 *env = (struct __env_1 *)__env;
    int i1 = env->i1;
    return i1;
}

int main() {
    int i1 = 0;
    int wsum = 0;
    while ((i1 < 3)) {
        struct __env_1 *f3_env = malloc(sizeof(struct __env_1));
        f3_env->i1 = i1;
        Closure_i32 f3 = {f3_env, __fn_1};
        int r12 = f3.fn(f3.env);
        wsum = (wsum + r12);;
        i1 = (i1 + 1);;
        free(f3.env);
    }
    int r13 = wsum;
    return 0;
}

----------------------------------------------------------------------
【测试结果】
----------------------------------------------------------------------
状态: PASS
编译: 通过
运行: 通过
期望输出: ['3']
实际输出: ['3']

======================================================================